<div class="map_box">
    <%= cl_image_tag space.photos.first.path || "person-placeholder.jpg", style: "width:200px; height:150px; border-radius: 5px;"%>
    <h3><%= space.name %></h3>
    <%= link_to "Check this space", space_path(space), class: "btn btn-success" %>
</div>

